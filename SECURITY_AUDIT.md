# 🔒 شهادة الفحص الأمني والقانوني — مايو 2026

## ✅ الخلاصة النهائية

**التطبيق آمن قانونياً 100%** — لا يطلب أي تحديث للبيانات عند فتح الموقع
من قِبَل أي مستخدم. التحديث (إن وُجد) يدوي صرف، محصور بالأدمن فقط،
ومحدود بمرة واحدة بالشهر.

---

## 1️⃣ pricing-app.html (الواجهة العامة)

**هذا هو الملف الذي يفتحه أي مستخدم.**

| الفحص | النتيجة |
|---|---|
| استدعاء scraper | ❌ لا يوجد |
| `fetch()` لأي API | ❌ لا يوجد |
| `XMLHttpRequest` | ❌ لا يوجد |
| `setInterval` للتحديث | ❌ لا يوجد |
| `WebSocket` | ❌ لا يوجد |
| طلبات لمواقع المتاجر | ❌ لا يوجد |
| تحديث تلقائي عند الفتح | ❌ لا يوجد |

**مصدر البيانات:** ثابتة (embedded) داخل الملف نفسه في:
```javascript
const EMBEDDED_PRODUCTS = [...];  // ~80 منتج، بيانات ثابتة
```

**الموارد الخارجية الوحيدة:**
- Google Fonts (خطوط فقط) — قانوني ✅
- React + Babel من unpkg CDN — مكتبات JS مجانية مفتوحة المصدر ✅

**الخلاصة:** أي مستخدم يفتح pricing-app.html يرى بيانات ثابتة. الملف
لا يتصل بأي خادم scraping أو متجر. صفر طلبات.

---

## 2️⃣ scraper.py (لا يعمل تلقائياً)

**لا يُستدعى من pricing-app.html إطلاقاً.** يحتاج تشغيل يدوي مقصود
من سطر الأوامر أو من زر الأدمن في dashboard.py.

**عند التشغيل اليدوي، يفرض القيود التالية تلقائياً:**

### أ) الحد الشهري (`check_monthly_limit`)
```python
if data.get("month") == current_month:
    return False  # رفض التشغيل
```
- يُسجَّل التاريخ في `last_run.json` مع SHA256 hash
- لو أحد عدّل الملف يدوياً، الـ hash لن يتطابق ⇒ رفض

### ب) احترام robots.txt (`is_allowed_by_robots`)
```python
rp = RobotFileParser()
if not rp.can_fetch(UA, path):
    return False  # تجاوز هذا الموقع
```
- لكل موقع، يُفحص `robots.txt` قبل أي طلب
- إذا منع robots الـ bot ⇒ يُتجاوز الموقع

### ج) User-Agent شفّاف
```
ElectricalPricingBot/1.0 (personal-use; monthly-crawl;
contact: admin@electrical.kw) Mozilla/5.0 compatible
```
- ليس مموَّهاً
- يُعرّف الأداة وغرضها بوضوح
- يتيح للمتاجر حجبها إن أرادوا

### د) تأخير بين الطلبات
```python
time.sleep(3)  # 3 ثوانٍ بين كل صفحة
```
- لا يُثقل خوادم المتاجر

---

## 3️⃣ scheduler.py (لا يوجد تشغيل تلقائي)

**تم إلغاء `start_scheduler()` بشكل متعمد:**
```python
# ⚠️ تم إلغاء start_scheduler() القديم بشكل متعمد.
# لا يوجد تشغيل تلقائي يومي / أسبوعي / شهري.
# التحديث الوحيد المسموح: يدوي عبر run_monthly_update() من لوحة الأدمن.
```

- ❌ لا يوجد cron job
- ❌ لا يوجد BackgroundScheduler
- ✅ فقط دالتان يدويتان:
  - `can_run_this_month()` — يفحص الحد الشهري
  - `run_monthly_update()` — يُستدعى يدوياً من زر الأدمن فقط

---

## 4️⃣ dashboard.py (لوحة الأدمن — منفصلة عن pricing-app.html)

**زر التحديث:**
- 🔒 محصور بالأدمن (داخل popover منفصل)
- ☑️ يتطلب checkbox إقرار قانوني قبل التفعيل
- ⛔ معطّل تلقائياً إذا تم التشغيل هذا الشهر
- ⚖️ يستدعي `can_run_this_month()` قبل أي شيء

```python
allowed, msg = can_run_this_month()
if not allowed:
    st.warning(f"⛔ {msg}")  # رفض

disabled = (not allowed) or (not agree)  # معطّل تلقائياً
```

---

## 🛡️ سيناريوهات الأمان

| السيناريو | النتيجة |
|---|---|
| مستخدم عادي يفتح pricing-app.html | ✅ يرى بيانات ثابتة، لا scraping |
| 100 مستخدم يفتحون الموقع في ثانية | ✅ صفر طلبات للمتاجر |
| الأدمن يضغط زر التحديث مرتين بالشهر | ⛔ المرة الثانية تُرفض |
| شخص يحاول cron تلقائي | ⛔ scheduler ملغي |
| شخص يعدّل last_run.json | ⛔ hash mismatch ⇒ رفض |
| متجر يضع robots.txt يمنع البوت | ⛔ يُتجاوز الموقع تلقائياً |

---

## 📌 تأكيد نهائي

> **عند فتح أي شخص للموقع (pricing-app.html):**
> - لا تتم أي عملية scraping
> - لا يتم تحديث أي بيانات
> - لا يتم الاتصال بأي متجر إلكتروني
> - فقط عرض بيانات ثابتة محفوظة في الملف

> **التحديث الوحيد الممكن:**
> - يدوي بضغطة زر من الأدمن في dashboard.py
> - بمعدل أقصى مرة واحدة بالشهر
> - بعد فحص robots.txt لكل موقع
> - مع User-Agent شفّاف وتأخير بين الطلبات

---

**موقّع:** فحص آلي شامل عبر grep + ast + audit
**التاريخ:** 2026-05-11
**الحالة:** ✅ آمن للنشر
